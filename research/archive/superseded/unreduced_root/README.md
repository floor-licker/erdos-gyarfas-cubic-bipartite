# Superseded unreduced-root counters

`unreduced_root_counts.tsv` predates root-stabilizer reduction of the first
augmentation. Its larger counters traverse symmetric copies; every
completion count is still zero.

Run `make verify-unreduced` to reproduce the table with both principal
implementations. It is retained only for provenance and regression testing,
not as primary evidence.
